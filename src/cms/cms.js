const CMS = require("decap-cms-app");
const { Widget } = require('decap-cms-widget-id');

// Do some fancy stuff with the CMS object here, lice CMS.registerMediaLibrary('');
CMS.registerWidget(Widget);